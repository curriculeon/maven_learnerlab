package com.github.curriculeon;

import org.junit.Assert;
import org.junit.Before;
import org.junit.Test;

import java.util.LinkedList;
import java.util.List;

import static org.junit.Assert.*;

/*public class TestPeople {

    People people = new People();
    Person person1, person2, person3, person4;

    @Before
    public void setup()
    {
        //given
        person1 = new Person(1, "Alice");
        person2 = new Person(2, "Bob");
        person3 = new Person(3, "John");
        person4 = new Person(4, "Eve");

        //when
        people.add(person1);
        people.add(person2);
        people.add(person3);
        people.add(person4);
    }

    @Test
    public void testAdd()
    {
        //then
        assertTrue(people.contains(person1));
        assertTrue(people.contains(person2));
        assertTrue(people.contains(person3));
        assertTrue(people.contains(person4));
    }

    @Test
    public void testRemove()
    {
        //when
        people.remove(person1);
        people.remove(person3);
        people.remove(person4);

        //then
        assertFalse(people.contains(person1));
        assertFalse(people.contains(person3));
        assertFalse(people.contains(person4));
    }

    public void testFindById(Person person, long id)
    {
        Assert.assertEquals(person, people.findById(id));
    }

    @Test
    public void testFindByID1(){testFindById(person1, 1);}

    @Test
    public void testFindById2(){testFindById(person2, 2);}

    @Test
    public void testFindById3(){testFindById(person3, 3);}

    @Test
    public void testFindById4(){testFindById(person4, 4);}

}*/
